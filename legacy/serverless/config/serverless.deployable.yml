# AUTO-GENERATED FILE. DO NOT EDIT DIRECTLY.
# Generated by scripts/generate-deployable-serverless.js
# Source: serverless.yml
service: hasivu-platform

frameworkVersion: '4'

provider:
  name: aws
  runtime: nodejs18.x
  region: ${opt:region, 'ap-south-1'}
  stage: ${opt:stage, 'dev'}
  memorySize: 512
  timeout: 30
  architecture: arm64

  environment:
    NODE_ENV: ${self:provider.stage}
    DATABASE_URL: ${ssm:/hasivu/${self:provider.stage}/database-url}
    AWS_COGNITO_USER_POOL_ID: ${ssm:/hasivu/${self:provider.stage}/cognito-user-pool-id}
    AWS_COGNITO_CLIENT_ID: ${ssm:/hasivu/${self:provider.stage}/cognito-client-id}
    RAZORPAY_KEY_ID: ${ssm:/hasivu/${self:provider.stage}/razorpay-key-id}
    RAZORPAY_KEY_SECRET: ${ssm:/hasivu/${self:provider.stage}/razorpay-key-secret}
    RAZORPAY_WEBHOOK_SECRET: ${ssm:/hasivu/${self:provider.stage}/razorpay-webhook-secret}
    WHATSAPP_ACCESS_TOKEN: ${ssm:/hasivu/${self:provider.stage}/whatsapp-access-token}
    FIREBASE_SERVER_KEY: ${ssm:/hasivu/${self:provider.stage}/firebase-server-key~true}
    PUSH_NOTIFICATION_TOPIC_ARN: ${cf:hasivu-${self:provider.stage}-messaging.PushNotificationTopicArn}
    S3_BUCKET_NAME: hasivu-${self:provider.stage}-uploads
    JWT_SECRET: ${ssm:/hasivu/${self:provider.stage}/jwt-secret~true}
    IDEMPOTENCY_TABLE: payment-webhook-idempotency-${self:provider.stage}
    DEAD_LETTER_QUEUE_URL: ${cf:hasivu-${self:provider.stage}-queues.DeadLetterQueueUrl}
    NOTIFICATION_TOPIC_ARN: ${cf:hasivu-${self:provider.stage}-messaging.NotificationTopicArn}
    RETRY_QUEUE_URL: ${cf:hasivu-${self:provider.stage}-queues.RetryQueueUrl}
    MAX_RETRY_ATTEMPTS: 3
    REDIS_URL: ${ssm:/hasivu/${self:provider.stage}/redis-url, ''}

  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - ssm:GetParameter
            - ssm:GetParameters
            - secretsmanager:GetSecretValue
          Resource:
            - 'arn:aws:ssm:${aws:region}:${aws:accountId}:parameter/hasivu/${self:provider.stage}/*'
            - 'arn:aws:secretsmanager:${aws:region}:${aws:accountId}:secret:hasivu/${self:provider.stage}/*'
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
          Resource: 'arn:aws:s3:::hasivu-${self:provider.stage}-uploads/*'
        - Effect: Allow
          Action:
            - cognito-idp:AdminCreateUser
            - cognito-idp:AdminSetUserPassword
            - cognito-idp:AdminUpdateUserAttributes
          Resource:
            - 'arn:aws:cognito-idp:${aws:region}:${aws:accountId}:userpool/${self:provider.environment.AWS_COGNITO_USER_POOL_ID}'
        - Effect: Allow
          Action:
            - ses:SendEmail
            - ses:SendRawEmail
          Resource: '*'
        - Effect: Allow
          Action:
            - logs:CreateLogGroup
            - logs:CreateLogStream
            - logs:PutLogEvents
          Resource:
            - 'arn:aws:logs:${aws:region}:${aws:accountId}:log-group:/aws/lambda/hasivu-${self:provider.stage}-*:*'
        - Effect: Allow
          Action:
            - dynamodb:GetItem
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
            - dynamodb:Query
            - dynamodb:Scan
          Resource:
            - 'arn:aws:dynamodb:${aws:region}:${aws:accountId}:table/payment-webhook-idempotency-${self:provider.stage}'
        - Effect: Allow
          Action:
            - sqs:SendMessage
            - sqs:ReceiveMessage
            - sqs:DeleteMessage
          Resource:
            - 'arn:aws:sqs:${aws:region}:${aws:accountId}:payment-retry-queue-${self:provider.stage}'
            - 'arn:aws:sqs:${aws:region}:${aws:accountId}:payment-dlq-${self:provider.stage}'
        - Effect: Allow
          Action:
            - sns:Publish
            - sns:CreatePlatformEndpoint
            - sns:DeleteEndpoint
            - sns:GetEndpointAttributes
            - sns:SetEndpointAttributes
          Resource:
            - 'arn:aws:sns:${aws:region}:${aws:accountId}:payment-notifications-${self:provider.stage}'
            - 'arn:aws:sns:${aws:region}:${aws:accountId}:push-notifications-${self:provider.stage}'
            - 'arn:aws:sns:${aws:region}:${aws:accountId}:app/*'
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
          Resource:
            - 'arn:aws:s3:::hasivu-${self:provider.stage}-ml-models/*'
            - 'arn:aws:s3:::hasivu-${self:provider.stage}-invoice-templates/*'
            - 'arn:aws:s3:::hasivu-${self:provider.stage}-invoices/*'
            - 'arn:aws:s3:::hasivu-${self:provider.stage}-analytics/*'
        - Effect: Allow
          Action:
            - sagemaker:InvokeEndpoint
          Resource:
            - 'arn:aws:sagemaker:${aws:region}:${aws:accountId}:endpoint/*'
        - Effect: Allow
          Action:
            - bedrock:InvokeModel
          Resource:
            - 'arn:aws:bedrock:${aws:region}::foundation-model/*'

  apiGateway:
    restApiId: ${ssm:/hasivu/${self:provider.stage}/api-gateway-id, ''}
    restApiRootResourceId: ${ssm:/hasivu/${self:provider.stage}/api-gateway-root-id, ''}
    throttle:
      rateLimit: 1000
      burstLimit: 2000
    metrics: true
    logs:
      restApi: true
      level: INFO
    cors:
      origin: ${self:custom.corsOrigin}
      headers:
        - Content-Type
        - X-Amz-Date
        - Authorization
        - X-Api-Key
        - X-Amz-Security-Token
        - X-Amz-User-Agent
      allowCredentials: false

  logs:
    restApi: true
    level: INFO

  tracing:
    lambda: true
    apiGateway: true

plugins:
  - serverless-offline
  - serverless-plugin-warmup
  - serverless-plugin-split-stacks
  - serverless-associate-waf

custom:
  corsOrigin: ${ssm:/hasivu/${self:provider.stage}/cors-origin, 'https://app.hasivu.com'}
  cognitoAuthorizerArn: arn:aws:cognito-idp:${aws:region}:${aws:accountId}:userpool/${self:provider.environment.AWS_COGNITO_USER_POOL_ID}

  warmup:
    enabled: true
    prewarm: true
    concurrency: 5

  splitStacks:
    perFunction: false
    perType: true

  associateWaf:
    name: hasivu-${self:provider.stage}-waf
    version: V2

functions:
  # Health Check
  auth-login:
    handler: src/functions/auth/login.loginHandler
    events:
      - http:
          path: /auth/login
          method: post
          cors: true
    environment:
      FUNCTION_NAME: auth-login

  auth-register:
    handler: src/functions/auth/register.registerHandler
    events:
      - http:
          path: /auth/register
          method: post
          cors: true
    environment:
      FUNCTION_NAME: auth-register
  auth-forgot-password:
    handler: src/functions/auth/forgot-password.forgotPasswordHandler
    events:
      - http:
          path: /auth/forgot-password
          method: post
          cors: true
    environment:
      FUNCTION_NAME: auth-forgot-password

  auth-logout:
    handler: src/functions/auth/logout.logoutHandler
    events:
      - http:
          path: /auth/logout
          method: post
          cors: true
    environment:
      FUNCTION_NAME: auth-logout

  # User Management Functions - Story 1.3 Core User Management
  users-list:
    handler: src/functions/users/getUsers.getUsersHandler
    events:
      - http:
          path: /api/v1/users
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: users-list

  users-get:
    handler: src/functions/users/getUserById.getUserByIdHandler
    events:
      - http:
          path: /api/v1/users/{id}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: users-get

  users-update:
    handler: src/functions/users/updateUser.updateUserHandler
    events:
      - http:
          path: /api/v1/users/{id}
          method: put
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: users-update

  users-bulk-import:
    handler: src/functions/users/bulkImport.bulkImportUsersHandler
    timeout: 60
    memorySize: 1024
    events:
      - http:
          path: /api/v1/users/bulk-import
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: users-bulk-import

  users-manage-children:
    handler: src/functions/users/manageChildren.manageChildrenHandler
    events:
      - http:
          path: /api/v1/users/{id}/children
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: users-manage-children

  # Payment Functions
  orders-create:
    handler: src/functions/orders/create-order.createOrderHandler
    memorySize: 1024
    events:
      - http:
          path: /orders
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: orders-create

  orders-get:
    handler: src/functions/orders/get-order.getOrderHandler
    events:
      - http:
          path: /orders/{orderId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: orders-get

  orders-update:
    handler: src/functions/orders/update-order.updateOrderHandler
    events:
      - http:
          path: /orders/{orderId}
          method: put
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: orders-update
  rfid-manage-readers:
    handler: src/functions/rfid/manage-readers.manageRfidReadersHandler
    events:
      - http:
          path: /rfid/readers
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /rfid/readers/{readerId}
          method: put
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /rfid/readers
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: rfid-manage-readers

  rfid-verify-card:
    handler: src/functions/rfid/verify-card.verifyRfidCardHandler
    events:
      - http:
          path: /rfid/verify
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: rfid-verify-card

  rfid-bulk-import-cards:
    handler: src/functions/rfid/bulk-import-cards.bulkImportCardsHandler
    timeout: 60
    memorySize: 1024
    events:
      - http:
          path: /rfid/cards/bulk-import
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: rfid-bulk-import-cards

  # Mobile Integration Functions - Story 2.4: Parent Mobile Integration
  mobile-parent-notifications:
    handler: src/functions/mobile/parent-notifications.parentNotificationsHandler
    events:
      - http:
          path: /api/v1/mobile/notifications/send
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /api/v1/mobile/notifications/{parentId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /api/v1/mobile/notifications/{notificationId}/read
          method: put
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: mobile-parent-notifications

  mobile-delivery-tracking:
    handler: src/functions/mobile/delivery-tracking.deliveryTrackingHandler
    events:
      - http:
          path: /api/v1/mobile/tracking/orders/{orderId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /api/v1/mobile/tracking/student/{studentId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: mobile-delivery-tracking

  mobile-device-registration:
    handler: src/functions/mobile/device-registration.deviceRegistrationHandler
    events:
      - http:
          path: /api/v1/mobile/devices/register
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /api/v1/mobile/devices/{deviceId}
          method: put
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /api/v1/mobile/devices/{deviceId}
          method: delete
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /api/v1/mobile/devices
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: mobile-device-registration

  rfid-test-connection:
    handler: src/functions/rfid/manage-readers.testRfidReaderHandler
    events:
      - http:
          path: /rfid/readers/{readerId}/test
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: rfid-test-connection
  rfid-delivery-verification:
    handler: src/functions/rfid/delivery-verification.deliveryVerificationHandler
    events:
      - http:
          path: /rfid/verify-delivery
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: rfid-delivery-verification
  rfid-mobile-tracking:
    handler: src/functions/rfid/mobile-tracking.mobileTrackingHandler
    events:
      - http:
          path: /rfid/tracking/{studentId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: rfid-mobile-tracking
  monitoring-dashboard:
    handler: src/functions/monitoring/dashboard.monitoringDashboardHandler
    events:
      - http:
          path: /monitoring/dashboard
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: monitoring-dashboard

  monitoring-health:
    handler: src/functions/monitoring/dashboard.serviceHealthHandler
    events:
      - http:
          path: /monitoring/health/{service}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: monitoring-health

  monitoring-metrics:
    handler: src/functions/monitoring/dashboard.systemMetricsHandler
    events:
      - http:
          path: /monitoring/metrics
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: monitoring-metrics

  monitoring-performance:
    handler: src/functions/monitoring/dashboard.performanceMetricsHandler
    events:
      - http:
          path: /monitoring/performance
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: monitoring-performance

  monitoring-business:
    handler: src/functions/monitoring/dashboard.businessMetricsHandler
    events:
      - http:
          path: /monitoring/business
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: monitoring-business

  monitoring-alerts:
    handler: src/functions/monitoring/dashboard.alertsHandler
    events:
      - http:
          path: /monitoring/alerts
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: monitoring-alerts

  monitoring-recommendations:
    handler: src/functions/monitoring/dashboard.recommendationsHandler
    events:
      - http:
          path: /monitoring/recommendations
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: monitoring-recommendations

  # =====================================================
  # Epic 6: Advanced Analytics & Insights - Lambda Functions
  # =====================================================

  # Analytics Orchestrator - Central hub for all analytics operations
  analytics-orchestrator:
    handler: src/functions/analytics/analytics-orchestrator.handler
    timeout: 180
    memorySize: 2048
    events:
      - http:
          path: /analytics/orchestrator/execute
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/orchestrator/health
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/orchestrator/status
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/orchestrator/cache-stats
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/orchestrator/batch
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-orchestrator
      ANALYTICS_DATA_BUCKET: hasivu-${self:provider.stage}-analytics

  # Business Intelligence Aggregator - Enterprise-level BI insights
  analytics-business-intelligence:
    handler: src/functions/analytics/business-intelligence-aggregator.handler
    timeout: 240
    memorySize: 3008
    events:
      - http:
          path: /analytics/business-intelligence/dashboard
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/business-intelligence/metrics
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/business-intelligence/trends
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/business-intelligence/insights
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-business-intelligence
      ML_MODEL_BUCKET: hasivu-${self:provider.stage}-ml-models

  # Cross-School Analytics - Multi-school comparative analysis
  analytics-cross-school:
    handler: src/functions/analytics/cross-school-analytics.handler
    timeout: 180
    memorySize: 2048
    events:
      - http:
          path: /analytics/cross-school/compare
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/cross-school/benchmarks
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/cross-school/rankings
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/cross-school/insights
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-cross-school

  # Executive Dashboard Engine - C-level insights and KPIs
  analytics-executive-dashboard:
    handler: src/functions/analytics/executive-dashboard-engine.handler
    timeout: 120
    memorySize: 2048
    events:
      - http:
          path: /analytics/executive/dashboard
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/executive/kpis
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/executive/strategic-metrics
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/executive/reports
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-executive-dashboard

  # Performance Benchmarking - School performance metrics
  analytics-performance-benchmarking:
    handler: src/functions/analytics/performance-benchmarking.handler
    timeout: 180
    memorySize: 2048
    events:
      - http:
          path: /analytics/performance/benchmark
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/performance/compare
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/performance/metrics
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/performance/trends
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-performance-benchmarking

  # Predictive Insights Engine - ML-powered forecasting
  analytics-predictive-insights:
    handler: src/functions/analytics/predictive-insights-engine.handler
    timeout: 240
    memorySize: 3008
    events:
      - http:
          path: /analytics/predictive/forecast
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/predictive/demand
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/predictive/enrollment
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/predictive/risk-assessment
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-predictive-insights
      ML_MODEL_BUCKET: hasivu-${self:provider.stage}-ml-models

  # Revenue Optimization Analyzer - Financial analytics
  analytics-revenue-optimization:
    handler: src/functions/analytics/revenue-optimization-analyzer.handler
    timeout: 180
    memorySize: 2048
    events:
      - http:
          path: /analytics/revenue/optimize
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/revenue/metrics
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/revenue/forecasts
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/revenue/recommendations
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-revenue-optimization

  # Strategic Insights Generator - AI-powered strategic recommendations
  analytics-strategic-insights:
    handler: src/functions/analytics/strategic-insights-generator.handler
    timeout: 240
    memorySize: 3008
    events:
      - http:
          path: /analytics/strategic/generate
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/strategic/insights
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/strategic/opportunities
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /analytics/strategic/risks
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: analytics-strategic-insights
      ML_MODEL_BUCKET: hasivu-${self:provider.stage}-ml-models

  # =====================================================
  # Epic 7: Intelligent Nutrition Management - Lambda Functions
  # =====================================================

  # Dietary Recommendation Engine - Personalized dietary recommendations
  nutrition-dietary-recommendations:
    handler: src/functions/nutrition/dietary-recommendation-engine.handler
    timeout: 120
    memorySize: 2048
    events:
      - http:
          path: /nutrition/recommendations/generate
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/recommendations/student/{studentId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/recommendations/class/{classId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/recommendations/personalized
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: nutrition-dietary-recommendations
      ML_MODEL_BUCKET: hasivu-${self:provider.stage}-ml-models

  # Meal Optimization AI - ML-powered meal planning optimization
  nutrition-meal-optimization:
    handler: src/functions/nutrition/meal-optimization-ai.handler
    timeout: 180
    memorySize: 2048
    events:
      - http:
          path: /nutrition/meal-optimization/optimize
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/meal-optimization/suggestions
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/meal-optimization/balance
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/meal-optimization/cost-efficiency
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: nutrition-meal-optimization
      ML_MODEL_BUCKET: hasivu-${self:provider.stage}-ml-models

  # Nutrition Analyzer - Comprehensive nutritional analysis
  nutrition-analyzer:
    handler: src/functions/nutrition/nutrition-analyzer.nutritionAnalyzerHandler
    timeout: 180
    memorySize: 2048
    events:
      - http:
          path: /nutrition/analyze
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/analysis/{analysisId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/analysis/school/{schoolId}
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/analysis/batch
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: nutrition-analyzer

  # Nutrition Compliance Checker - Regulatory compliance validation
  nutrition-compliance:
    handler: src/functions/nutrition/nutrition-compliance-checker.handler
    timeout: 120
    memorySize: 1024
    events:
      - http:
          path: /nutrition/compliance/check
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/compliance/report
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/compliance/standards
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/compliance/violations
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: nutrition-compliance

  # Nutritional Trend Analyzer - Historical nutrition trends analysis
  nutrition-trend-analyzer:
    handler: src/functions/nutrition/nutritional-trend-analyzer.handler
    timeout: 180
    memorySize: 2048
    events:
      - http:
          path: /nutrition/trends/analyze
          method: post
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/trends/historical
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/trends/patterns
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
      - http:
          path: /nutrition/trends/forecasts
          method: get
          cors: true
          authorizer: ${self:custom.cognitoAuthorizerArn}
    environment:
      FUNCTION_NAME: nutrition-trend-analyzer

resources:
  Resources:
    # S3 Bucket for uploads
    S3BucketUploads:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: hasivu-${self:provider.stage}-uploads
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true
        BucketEncryption:
          ServerSideEncryptionConfiguration:
            - ServerSideEncryptionByDefault:
                SSEAlgorithm: AES256
        VersioningConfiguration:
          Status: Enabled
        LifecycleConfiguration:
          Rules:
            - Status: Enabled
              ExpirationInDays: 365
              NoncurrentVersionExpirationInDays: 30

    # S3 Bucket for ML models
    S3BucketMLModels:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: hasivu-${self:provider.stage}-ml-models
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true
        BucketEncryption:
          ServerSideEncryptionConfiguration:
            - ServerSideEncryptionByDefault:
                SSEAlgorithm: AES256
        VersioningConfiguration:
          Status: Enabled
        LifecycleConfiguration:
          Rules:
            - Status: Enabled
              ExpirationInDays: 180
              NoncurrentVersionExpirationInDays: 30

    # S3 Bucket for invoice templates
    S3BucketInvoiceTemplates:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: hasivu-${self:provider.stage}-invoice-templates
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true
        BucketEncryption:
          ServerSideEncryptionConfiguration:
            - ServerSideEncryptionByDefault:
                SSEAlgorithm: AES256
        VersioningConfiguration:
          Status: Enabled

    # S3 Bucket for generated invoices
    S3BucketInvoices:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: hasivu-${self:provider.stage}-invoices
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true
        BucketEncryption:
          ServerSideEncryptionConfiguration:
            - ServerSideEncryptionByDefault:
                SSEAlgorithm: AES256
        VersioningConfiguration:
          Status: Enabled
        LifecycleConfiguration:
          Rules:
            - Status: Enabled
              ExpirationInDays: 2555 # 7 years for tax compliance
              NoncurrentVersionExpirationInDays: 90

    # S3 Bucket for analytics data
    S3BucketAnalytics:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: hasivu-${self:provider.stage}-analytics
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true
        BucketEncryption:
          ServerSideEncryptionConfiguration:
            - ServerSideEncryptionByDefault:
                SSEAlgorithm: AES256
        VersioningConfiguration:
          Status: Enabled
        LifecycleConfiguration:
          Rules:
            - Status: Enabled
              ExpirationInDays: 1095 # 3 years
              NoncurrentVersionExpirationInDays: 30

    # CloudWatch Log Groups
    HealthLogGroup:
      Type: AWS::Logs::LogGroup
      Properties:
        LogGroupName: /aws/lambda/hasivu-${self:provider.stage}-health
        RetentionInDays: 30

    # WAF for API Gateway
    WebACL:
      Type: AWS::WAFv2::WebACL
      Properties:
        Name: hasivu-${self:provider.stage}-waf
        Scope: REGIONAL
        DefaultAction:
          Allow: {}
        Rules:
          - Name: RateLimitRule
            Priority: 1
            Statement:
              RateBasedStatement:
                Limit: 2000
                AggregateKeyType: IP
            Action:
              Block: {}
            VisibilityConfig:
              SampledRequestsEnabled: true
              CloudWatchMetricsEnabled: true
              MetricName: RateLimitRule
          - Name: GeoBlockRule
            Priority: 2
            Statement:
              GeoMatchStatement:
                CountryCodes: [IN]
            Action:
              Allow: {}
            VisibilityConfig:
              SampledRequestsEnabled: true
              CloudWatchMetricsEnabled: true
              MetricName: GeoBlockRule
        VisibilityConfig:
          SampledRequestsEnabled: true
          CloudWatchMetricsEnabled: true
          MetricName: hasivu-${self:provider.stage}-waf

    # Epic 5 Payment System Resources

    # DynamoDB table for webhook idempotency
    PaymentWebhookIdempotencyTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: payment-webhook-idempotency-${self:provider.stage}
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: webhookId
            AttributeType: S
        KeySchema:
          - AttributeName: webhookId
            KeyType: HASH
        TimeToLiveSpecification:
          AttributeName: ttl
          Enabled: true
        StreamSpecification:
          StreamViewType: NEW_AND_OLD_IMAGES

    # SQS Dead Letter Queue for failed payments
    PaymentDeadLetterQueue:
      Type: AWS::SQS::Queue
      Properties:
        QueueName: payment-dlq-${self:provider.stage}
        MessageRetentionPeriod: 1209600 # 14 days
        VisibilityTimeoutSeconds: 300

    # SQS Retry Queue for payment retries
    PaymentRetryQueue:
      Type: AWS::SQS::Queue
      Properties:
        QueueName: payment-retry-queue-${self:provider.stage}
        VisibilityTimeoutSeconds: 60
        MessageRetentionPeriod: 1209600 # 14 days
        RedrivePolicy:
          deadLetterTargetArn: !GetAtt PaymentDeadLetterQueue.Arn
          maxReceiveCount: 3

    # SNS Topic for payment notifications
    PaymentNotificationsTopic:
      Type: AWS::SNS::Topic
      Properties:
        TopicName: payment-notifications-${self:provider.stage}
        DisplayName: Payment Notifications
        FifoTopic: false

    # SNS Topic for mobile push notifications
    PushNotificationsTopic:
      Type: AWS::SNS::Topic
      Properties:
        TopicName: push-notifications-${self:provider.stage}
        DisplayName: Mobile Push Notifications
        FifoTopic: false

    # CloudWatch Dashboard
    CloudWatchDashboard:
      Type: AWS::CloudWatch::Dashboard
      Properties:
        DashboardName: hasivu-${self:provider.stage}-metrics
        DashboardBody: |
          {
            "widgets": [
              {
                "type": "metric",
                "properties": {
                  "metrics": [
                    [ "AWS/Lambda", "Duration", "FunctionName", "hasivu-${self:provider.stage}-health" ],
                    [ ".", "Errors", ".", "." ],
                    [ ".", "Invocations", ".", "." ]
                  ],
                  "period": 300,
                  "stat": "Average",
                  "region": "${aws:region}",
                  "title": "Lambda Metrics"
                }
              },
              {
                "type": "metric",
                "properties": {
                  "metrics": [
                    [ "AWS/ApiGateway", "Count", "ApiName", "hasivu-${self:provider.stage}" ],
                    [ ".", "Latency", ".", "." ],
                    [ ".", "4XXError", ".", "." ],
                    [ ".", "5XXError", ".", "." ]
                  ],
                  "period": 300,
                  "stat": "Sum",
                  "region": "${aws:region}",
                  "title": "API Gateway Metrics"
                }
              },
              {
                "type": "metric",
                "properties": {
                  "metrics": [
                    [ "AWS/Lambda", "Duration", "FunctionName", "hasivu-${self:provider.stage}-payments-advanced" ],
                    [ ".", "Errors", ".", "." ],
                    [ ".", "Invocations", ".", "." ],
                    [ ".", "Duration", "FunctionName", "hasivu-${self:provider.stage}-payments-webhook-handler" ],
                    [ ".", "Errors", ".", "." ],
                    [ ".", "Invocations", ".", "." ]
                  ],
                  "period": 300,
                  "stat": "Average",
                  "region": "${aws:region}",
                  "title": "Payment System Metrics"
                }
              },
              {
                "type": "metric",
                "properties": {
                  "metrics": [
                    [ "AWS/SQS", "NumberOfMessagesSent", "QueueName", "payment-retry-queue-${self:provider.stage}" ],
                    [ ".", "NumberOfMessagesReceived", ".", "." ],
                    [ ".", "NumberOfMessagesSent", "QueueName", "payment-dlq-${self:provider.stage}" ]
                  ],
                  "period": 300,
                  "stat": "Sum",
                  "region": "${aws:region}",
                  "title": "Payment Queue Metrics"
                }
              }
            ]
          }

  Outputs:
    ApiGatewayRestApiId:
      Value: !Ref ApiGatewayRestApi
      Export:
        Name: hasivu-${self:provider.stage}-api-gateway-id

    ApiGatewayRestApiRootResourceId:
      Value: !GetAtt ApiGatewayRestApi.RootResourceId
      Export:
        Name: hasivu-${self:provider.stage}-api-gateway-root-id

    S3BucketName:
      Value: !Ref S3BucketUploads
      Export:
        Name: hasivu-${self:provider.stage}-s3-bucket-name

    # Epic 5 Payment System Outputs
    PaymentIdempotencyTableName:
      Value: !Ref PaymentWebhookIdempotencyTable
      Export:
        Name: hasivu-${self:provider.stage}-payment-idempotency-table

    PaymentRetryQueueUrl:
      Value: !Ref PaymentRetryQueue
      Export:
        Name: hasivu-${self:provider.stage}-payment-retry-queue-url

    PaymentDeadLetterQueueUrl:
      Value: !Ref PaymentDeadLetterQueue
      Export:
        Name: hasivu-${self:provider.stage}-payment-dlq-url

    PaymentNotificationsTopicArn:
      Value: !Ref PaymentNotificationsTopic
      Export:
        Name: hasivu-${self:provider.stage}-payment-notifications-topic

    PushNotificationsTopicArn:
      Value: !Ref PushNotificationsTopic
      Export:
        Name: hasivu-${self:provider.stage}-push-notifications-topic

    # Epic 5 Story 5.4: ML Models Bucket
    MLModelsBucketName:
      Value: !Ref S3BucketMLModels
      Export:
        Name: hasivu-${self:provider.stage}-ml-models-bucket-name

    # Epic 5 Story 5.3: Invoice System Buckets
    InvoiceTemplatesBucketName:
      Value: !Ref S3BucketInvoiceTemplates
      Export:
        Name: hasivu-${self:provider.stage}-invoice-templates-bucket-name

    InvoicesBucketName:
      Value: !Ref S3BucketInvoices
      Export:
        Name: hasivu-${self:provider.stage}-invoices-bucket-name

    AnalyticsBucketName:
      Value: !Ref S3BucketAnalytics
      Export:
        Name: hasivu-${self:provider.stage}-analytics-bucket-name
