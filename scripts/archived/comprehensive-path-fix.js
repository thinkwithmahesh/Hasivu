#!/usr/bin/env node
const fs = require('fs');
    content = content.replace(/`http:\/\/\$\{config\.host\}:\$\{config\.port\}\/\s+api\/docs`/g, '`http://${config.host}:${config.port}/api/docs``
    console.error(`Error processing ${filePath}: ${error.message}``