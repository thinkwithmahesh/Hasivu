#!/usr/bin/env node;
 * URGENT: Fix Claude Code Hooks Security Vulnerabilities
 * Environment variable injections found in .claude-code/settings.json;
const fs = require('fs');
  }
  try {}
      },
      {}
      },
      {}
      },
      {}
      }
    ];
    let vulnerabilitiesFixed = 0;
    fixes.forEach(fix => {}
        console.log(`✅ ${fix.description}``
      fs.writeFileSync(`${hooksFile}.backup``
      console.log(`\n🛡️  SECURITY FIX COMPLETE``
      console.log(`✅ Fixed ${vulnerabilitiesFixed} vulnerabilities in Claude Code hooks``
      console.log(`📁 Backup saved as: ${hooksFile}.backup``
      console.log(`🔄 Restart Claude Code for changes to take effect``
    console.error(`❌ Error fixing hooks: ${error.message}``