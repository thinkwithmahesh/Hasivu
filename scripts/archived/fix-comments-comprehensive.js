#!/usr/bin/env node
const fs = require('fs');
const path = require('path');
    }
    return hasChanges;
  }
{}
    console.error(`Error processing ${filePath}: ${error.message}``
          console.log(`Fixed: ${fullPath}``
console.log(`✅ Fixed ${fixed} files with corrupted comments``