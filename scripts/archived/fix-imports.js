#!/usr/bin/env node
const fs = require('fs');
const path = require('path');
    }
    return false;
  }
{}
    console.error(`Error processing ${filePath}: ${error.message}``
    console.log(`Fixed: ${file}``
console.log(`✅ Fixed ${totalFixed} files with corrupted import paths``