/**
 * Shared Validation Service
 * Re-export of the main validation service for shared functions
 */

export { ValidationService, ValidationService as default } from '../services/validation.service';
