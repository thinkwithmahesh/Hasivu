# Handoff procedures (stub)

**Status:** Linked from [../README.md](../README.md) — replace with team-specific process.

**Minimum sections to add:**

1. Branch / release naming and protected branches
2. Where PRD shards live (`docs/prd/`) and how to update RTM
3. Secrets handoff (SSM paths per `serverless.yml` conventions)
4. On-call and rollback pointer to `docs/runbooks/ROLLBACK.md`
