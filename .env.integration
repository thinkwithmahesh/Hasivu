# HASIVU Platform - Integration Test Environment Variables
# SQLite configuration for integration tests

# Node Environment
NODE_ENV=test

# Database Configuration (SQLite for tests)
DATABASE_URL="file:./test.db"

# Redis Configuration (Optional - use in-memory for tests)
REDIS_URL=redis://localhost:6379

# AWS Configuration (Mock values for tests)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test-key
AWS_SECRET_ACCESS_KEY=test-secret

# AWS Cognito Configuration (Mock values)
COGNITO_USER_POOL_ID=test-pool-id
COGNITO_CLIENT_ID=test-client-id

# Razorpay Configuration (Mock values)
RAZORPAY_KEY_ID=rzp_test_key
RAZORPAY_KEY_SECRET=rzp_test_secret
RAZORPAY_WEBHOOK_SECRET=test_webhook_secret

# Server Configuration
PORT=3001
HOST=localhost

# JWT Configuration
JWT_SECRET=test_jwt_secret_key_for_integration_tests
JWT_REFRESH_SECRET=test_refresh_secret_key

# Session Configuration
SESSION_SECRET=test_session_secret_key

# Email Configuration (Mock)
SMTP_HOST=localhost
SMTP_PORT=1025

# Test-specific settings
TEST_ENVIRONMENT=integration
LOG_LEVEL=error